name: Validate Sims Tan
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check JavaScript syntax
        run: |
          node --input-type=module --check < game-real.js
          node --input-type=module --check < premium-overrides.js
          node --input-type=module --check < bootstrap.js
      - name: Verify playable entrypoint
        run: |
          test -s index.html
          test -s style.css
          test -s game-real.js
          test -s premium-overrides.js
          test -s bootstrap.js
          grep -q 'bootstrap.js' index.html
          grep -q 'Televisor' game-real.js
          grep -q 'Ducha' game-real.js
          grep -q 'Cama' game-real.js
          grep -q 'vitruvian_body.glb' premium-overrides.js
          grep -q 'vitruvian_head.glb' premium-overrides.js
          grep -q 'Multijugador' index.html
      - name: Verify public no-login CDN URL
        run: |
          URL="https://cdn.jsdelivr.net/gh/${GITHUB_REPOSITORY}@${GITHUB_SHA}/index.html"
          for i in 1 2 3 4 5 6 7 8; do
            if curl -fsSL --max-time 20 "$URL" | grep -q 'bootstrap.js'; then
              echo "Public build available at $URL"
              exit 0
            fi
            sleep 3
          done
          exit 1
